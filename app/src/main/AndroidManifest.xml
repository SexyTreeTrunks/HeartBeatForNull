<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sookmyung.heartbeatfornull">

    <uses-permission
        android:name="android.permission.INTERNET">
    </uses-permission>
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION">
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />


        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".virtualtravel.Show2DMap"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <activity android:name=".searchpath.SelectLocationActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <activity android:name=".searchpath.ResultMapFromSearch"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <activity android:name=".Footprint"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <activity android:name=".Community"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <activity android:name=".virtualtravel.StreetViewInVirtualTravel"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
    </application>

</manifest>